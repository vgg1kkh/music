import styled from "styled-components";
const SongCoverWrapper = styled.a`
 width:${props=>props.width}+'px';
 .cover_wrapper {
     
    img {
     width: 100%;
 }
 }
 
`

export default SongCoverWrapper