import React, { memo } from 'react'

export default memo(function HotRecommend(props) {

    //state
    const {history} = props

    

    return (
        <div>
            HotRecommend
        </div>
    )
})
